version: '3.8'

services:
  sportpredict:
    build: .
    container_name: sportpredict-ai
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
